# دليل شامل لهيكل مشروع Instagram Clone

## نظرة عامة على المشروع

هذا المشروع هو تطبيق Instagram Clone مطور باستخدام Flutter مع Supabase كخدمة Backend. يستخدم المشروع نمط MVC مع GetX لإدارة الحالة والتنقل.

## التقنيات والأدوات المستخدمة

### Frontend Framework
- **Flutter**: إطار العمل الرئيسي لتطوير التطبيق
- **Dart**: لغة البرمجة المستخدمة

### إدارة الحالة والتنقل
- **GetX**: مكتبة شاملة لإدارة الحالة، التنقل، وحقن التبعيات

### Backend والقاعدة
- **Supabase**: منصة Backend-as-a-Service تتضمن:
  - PostgreSQL Database
  - Authentication
  - Real-time subscriptions
  - File Storage

### المكتبات الإضافية
- **cached_network_image**: تحسين عرض الصور من الإنترنت
- **image_picker**: اختيار الصور من المعرض أو الكاميرا
- **timeago**: عرض الأوقات النسبية
- **photo_view**: عرض الصور بملء الشاشة
- **flutter_slidable**: إضافة إجراءات السحب
- **shared_preferences**: تخزين البيانات محلياً
- **email_validator**: التحقق من صحة البريد الإلكتروني

---

## هيكل المجلدات والملفات

### 📁 المجلد الجذر

#### `pubspec.yaml`
**الوظيفة**: ملف تكوين المشروع الرئيسي
**المحتوى**:
- معلومات المشروع (الاسم، الإصدار، الوصف)
- التبعيات المطلوبة (dependencies)
- تبعيات التطوير (dev_dependencies)
- إعدادات الأصول (assets)
- إعدادات أيقونة التطبيق

**الأدوات المستخدمة**:
- get: ^4.6.6 (إدارة الحالة)
- supabase_flutter: ^2.5.6 (Backend)
- image_picker: ^1.0.7 (اختيار الصور)
- cached_network_image: ^3.3.1 (تحسين الصور)
- timeago: ^3.6.1 (عرض الأوقات)

**الترابطات**: يحدد جميع التبعيات المستخدمة في المشروع

---

#### `lib/main.dart`
**الوظيفة**: نقطة البداية الرئيسية للتطبيق
**المحتوى**:
- تهيئة Supabase
- تهيئة الخدمات الأساسية
- إعداد التطبيق الرئيسي
- تحديد المسار الأولي حسب حالة المصادقة

**الأدوات المستخدمة**:
- GetMaterialApp (التطبيق الرئيسي)
- Supabase.initialize() (تهيئة قاعدة البيانات)
- StorageService (تخزين الإعدادات)
- ConnectivityService (مراقبة الاتصال)

**الترابطات**:
- يستورد جميع الخدمات الأساسية
- يربط مع app_routes.dart للتنقل
- يربط مع app_translations.dart للترجمة
- يربط مع app_constants.dart للثوابت

---

### 📁 lib/constants/

#### `app_constants.dart`
**الوظيفة**: تخزين جميع الثوابت المستخدمة في التطبيق
**المحتوى**:
- معلومات التطبيق (الاسم، الإصدار)
- إعدادات Supabase (URL, API Key)
- أسماء الجداول في قاعدة البيانات
- مسارات التنقل
- ثوابت التحقق من صحة البيانات
- ثوابت واجهة المستخدم

**الترابطات**: يستخدم في جميع أنحاء التطبيق لضمان الاتساق

---

### 📁 lib/routes/

#### `app_routes.dart`
**الوظيفة**: تعريف جميع مسارات التنقل في التطبيق
**المحتوى**:
- تعريف أسماء المسارات
- ربط كل مسار بصفحته المقابلة
- تحديد الوسطاء (Middleware) لكل مسار
- ربط التبعيات (Bindings) لكل صفحة

**الأدوات المستخدمة**:
- GetPage (تعريف الصفحات)
- AuthMiddleware (التحقق من المصادقة)
- Bindings (حقن التبعيات)

**الترابطات**:
- يربط مع جميع صفحات التطبيق
- يستخدم AuthMiddleware للحماية
- يربط مع Controllers عبر Bindings

---

### 📁 lib/services/

#### `auth_service.dart`
**الوظيفة**: إدارة عمليات المصادقة والجلسات
**المحتوى**:
- مراقبة تغييرات حالة المصادقة
- التحقق من تأكيد البريد الإلكتروني
- جلب بيانات المستخدم
- تسجيل الخروج
- تنظيف Controllers عند الخروج

**الأدوات المستخدمة**:
- Supabase Auth
- GetX Service
- Stream Listener

**الترابطات**:
- يتفاعل مع Supabase للمصادقة
- يتحكم في التنقل حسب حالة المصادقة
- ينظف Controllers عند الخروج

#### `posts_service.dart`
**الوظيفة**: إدارة عمليات المنشورات
**المحتوى**:
- جلب المنشورات من قاعدة البيانات
- إنشاء منشورات جديدة
- رفع الصور إلى التخزين
- إدارة الإعجابات
- حذف المنشورات

**الأدوات المستخدمة**:
- Supabase Database
- Supabase Storage
- HTTP requests

#### `comments_service.dart`
**الوظيفة**: إدارة نظام التعليقات
**المحتوى**:
- جلب التعليقات
- إضافة تعليقات جديدة
- الرد على التعليقات
- حذف التعليقات

#### `user_profile_service.dart`
**الوظيفة**: إدارة ملفات المستخدمين
**المحتوى**:
- جلب بيانات المستخدمين
- تحديث الملف الشخصي
- إدارة المتابعة
- رفع صور الملف الشخصي

#### `storage_service.dart`
**الوظيفة**: إدارة التخزين المحلي
**المحتوى**:
- حفظ الإعدادات محلياً
- إدارة اللغة والثيم
- تخزين تفضيلات المستخدم

**الأدوات المستخدمة**:
- SharedPreferences
- GetX Service

#### `connectivity_service.dart`
**الوظيفة**: مراقبة حالة الاتصال بالإنترنت
**المحتوى**:
- مراقبة الاتصال في الوقت الفعلي
- إشعار المستخدم عند انقطاع الاتصال
- إعادة المحاولة عند عودة الاتصال

---

### 📁 lib/controllers/

#### `auth_controller.dart`
**الوظيفة**: التحكم في واجهة المصادقة ومنطق تسجيل الدخول/التسجيل
**المحتوى**:
- إدارة نماذج تسجيل الدخول والتسجيل
- التحقق من صحة البيانات
- التعامل مع أخطاء المصادقة
- إدارة حالة التحميل

**الأدوات المستخدمة**:
- GetX Controller
- TextEditingController
- Form Validation
- Supabase Auth

**الترابطات**:
- يتفاعل مع AuthService
- يتحكم في AuthPage
- يستخدم Validators للتحقق

#### `home_controller.dart`
**الوظيفة**: التحكم في الصفحة الرئيسية والتنقل بين التبويبات
**المحتوى**:
- إدارة التبويبات السفلية
- التحكم في الصفحة النشطة
- إدارة حالة التطبيق العامة

#### `posts_controller.dart`
**الوظيفة**: التحكم في عرض وإدارة المنشورات
**المحتوى**:
- جلب وعرض المنشورات
- إدارة الإعجابات
- التنقل إلى التعليقات
- تحديث المنشورات في الوقت الفعلي

#### `create_post_controller.dart`
**الوظيفة**: التحكم في إنشاء المنشورات الجديدة
**المحتوى**:
- إدارة نموذج إنشاء المنشور
- اختيار ومعاينة الصور
- رفع المنشورات إلى الخادم
- التحقق من صحة البيانات

#### `comments_controller.dart`
**الوظيفة**: التحكم في نظام التعليقات
**المحتوى**:
- جلب وعرض التعليقات
- إضافة تعليقات جديدة
- إدارة الردود على التعليقات
- حذف التعليقات

#### `profile_edit_controller.dart`
**الوظيفة**: التحكم في تعديل الملف الشخصي
**المحتوى**:
- إدارة نموذج تعديل الملف
- رفع صورة الملف الشخصي
- تحديث بيانات المستخدم
- التحقق من صحة البيانات

#### `user_profile_controller.dart`
**الوظيفة**: التحكم في عرض ملفات المستخدمين الآخرين
**المحتوى**:
- جلب بيانات المستخدم
- عرض منشورات المستخدم
- إدارة المتابعة/إلغاء المتابعة
- عرض إحصائيات المستخدم

#### `my_posts_controller.dart`
**الوظيفة**: التحكم في عرض منشورات المستخدم الشخصية
**المحتوى**:
- جلب منشورات المستخدم الحالي
- حذف المنشورات
- تعديل المنشورات
- إدارة الخصوصية

#### `settings_controller.dart`
**الوظيفة**: التحكم في إعدادات التطبيق
**المحتوى**:
- إدارة الثيم (فاتح/داكن)
- تغيير اللغة
- إعدادات الإشعارات
- إعدادات الخصوصية

---

### 📁 lib/views/

#### `auth_page.dart`
**الوظيفة**: واجهة تسجيل الدخول والتسجيل
**المحتوى**:
- نماذج تسجيل الدخول والتسجيل
- التبديل بين الوضعين
- عرض رسائل الخطأ
- أزرار التحكم

**الأدوات المستخدمة**:
- GetX Obx للتفاعل
- TextFormField للإدخال
- ElevatedButton للأزرار

**الترابطات**:
- يتحكم بها AuthController
- يستخدم Validators للتحقق

#### `home_page.dart`
**الوظيفة**: الصفحة الرئيسية مع التنقل السفلي
**المحتوى**:
- شريط التنقل السفلي
- عرض الصفحات المختلفة
- إدارة التبويبات

**الأدوات المستخدمة**:
- CurvedNavigationBar
- PageView أو IndexedStack
- GetX للتحكم

#### `feed_page.dart`
**الوظيفة**: صفحة عرض المنشورات الرئيسية
**المحتوى**:
- قائمة المنشورات
- تبويبات الفلترة
- تحديث السحب للأسفل
- تحميل المزيد

**الأدوات المستخدمة**:
- ListView.builder
- RefreshIndicator
- FilterTabs widget

#### `create_post_page.dart`
**الوظيفة**: صفحة إنشاء المنشورات
**المحتوى**:
- نموذج إنشاء المنشور
- اختيار الصور
- معاينة المحتوى
- نشر المنشور

**الأدوات المستخدمة**:
- TextFormField
- ImagePicker
- MediaCarousel widget

#### `comments_page.dart`
**الوظيفة**: صفحة عرض وإدارة التعليقات
**المحتوى**:
- عرض التعليقات والردود
- إضافة تعليقات جديدة
- الرد على التعليقات
- حذف التعليقات

**الأدوات المستخدمة**:
- CommentThread widget
- ListView للتعليقات
- TextField للإدخال

#### `profile_edit_page.dart`
**الوظيفة**: صفحة تعديل الملف الشخصي
**المحتوى**:
- نموذج تعديل البيانات
- رفع صورة الملف
- حفظ التغييرات
- معاينة الملف

#### `user_profile_page.dart`
**الوظيفة**: صفحة عرض ملف المستخدم
**المحتوى**:
- معلومات المستخدم
- منشورات المستخدم
- زر المتابعة
- إحصائيات الملف

#### `my_posts_page.dart`
**الوظيفة**: صفحة منشورات المستخدم الشخصية
**المحتوى**:
- عرض منشورات المستخدم
- خيارات التعديل والحذف
- إحصائيات المنشورات

#### `settings_page.dart`
**الوظيفة**: صفحة إعدادات التطبيق
**المحتوى**:
- إعدادات الثيم
- إعدادات اللغة
- إعدادات الإشعارات
- إعدادات الخصوصية

---

### 📁 lib/widgets/

#### `post_card.dart`
**الوظيفة**: مكون عرض المنشور الواحد
**المحتوى**:
- صورة وبيانات المستخدم
- محتوى المنشور (نص/صور)
- أزرار التفاعل (إعجاب، تعليق، مشاركة)
- عرض عدد الإعجابات والتعليقات

**الأدوات المستخدمة**:
- Card widget
- CachedNetworkImage
- IconButton للتفاعل
- Timeago للوقت

**الترابطات**:
- يستخدم في FeedPage
- يتفاعل مع PostsController
- يستخدم MediaCarousel للصور

#### `comment_thread.dart`
**الوظيفة**: مكون عرض التعليق مع الردود
**المحتوى**:
- عرض التعليق الأساسي
- عرض الردود المتداخلة
- خيارات الرد والحذف
- معلومات المعلق

**الأدوات المستخدمة**:
- ExpansionTile للردود
- ListTile للتعليقات
- PopupMenuButton للخيارات

#### `media_carousel.dart`
**الوظيفة**: مكون عرض الصور المتعددة
**المحتوى**:
- عرض الصور في شكل carousel
- مؤشرات الصور
- إمكانية التكبير
- التنقل بين الصور

**الأدوات المستخدمة**:
- PageView للتنقل
- PhotoView للتكبير
- Indicators للمؤشرات

#### `filter_tabs.dart`
**الوظيفة**: مكون تبويبات الفلترة
**المحتوى**:
- تبويبات مختلفة (الكل، الأحدث، الأكثر شعبية)
- تغيير الفلتر النشط
- تحديث المحتوى حسب الفلتر

#### `connectivity_widget.dart`
**الوظيفة**: مكون عرض حالة الاتصال
**المحتوى**:
- رسالة عدم الاتصال
- زر إعادة المحاولة
- مؤشر حالة الاتصال

**الأدوات المستخدمة**:
- GetX Obx للتفاعل
- ConnectivityService للحالة

---

### 📁 lib/models/

#### `user_profile_model.dart`
**الوظيفة**: نموذج بيانات المستخدم
**المحتوى**:
- معرف المستخدم
- الاسم الكامل واسم المستخدم
- البريد الإلكتروني
- صورة الملف الشخصي
- السيرة الذاتية
- إحصائيات المتابعة

**الترابطات**:
- يستخدم في جميع Controllers المتعلقة بالمستخدمين
- يتفاعل مع Supabase Users table

#### `post_model.dart`
**الوظيفة**: نموذج بيانات المنشور
**المحتوى**:
- معرف المنشور
- محتوى المنشور
- صور المنشور
- معلومات المؤلف
- تاريخ الإنشاء
- إحصائيات التفاعل

#### `comment_model.dart`
**الوظيفة**: نموذج بيانات التعليق
**المحتوى**:
- معرف التعليق
- محتوى التعليق
- معلومات المعلق
- معرف المنشور
- معرف التعليق الأب (للردود)
- تاريخ الإنشاء

---

### 📁 lib/utils/

#### `validators.dart`
**الوظيفة**: دوال التحقق من صحة البيانات
**المحتوى**:
- التحقق من البريد الإلكتروني
- التحقق من كلمة المرور
- التحقق من اسم المستخدم
- التحقق من الاسم الكامل

**الأدوات المستخدمة**:
- email_validator package
- RegExp للتحقق من الأنماط

#### `error_handler.dart`
**الوظيفة**: إدارة الأخطاء وعرضها
**المحتوى**:
- تحويل أخطاء Supabase إلى رسائل مفهومة
- عرض رسائل الخطأ للمستخدم
- تسجيل الأخطاء للتطوير

#### `auth_helper.dart`
**الوظيفة**: دوال مساعدة للمصادقة
**المحتوى**:
- التحقق من حالة المصادقة
- استخراج بيانات المستخدم
- إدارة الجلسات

---

### 📁 lib/middleware/

#### `auth_middleware.dart`
**الوظيفة**: التحقق من المصادقة قبل الوصول للصفحات
**المحتوى**:
- فحص وجود مستخدم مسجل
- إعادة التوجيه لصفحة تسجيل الدخول
- السماح بالوصول للمستخدمين المصادق عليهم

**الأدوات المستخدمة**:
- GetMiddleware
- Supabase Auth

---

### 📁 lib/bindings/

#### `auth_binding.dart`
**الوظيفة**: حقن تبعيات صفحة المصادقة
**المحتوى**:
- إنشاء AuthController
- إنشاء AuthService
- ربط التبعيات المطلوبة

#### `home_binding.dart`
**الوظيفة**: حقن تبعيات الصفحة الرئيسية
**المحتوى**:
- إنشاء HomeController
- إنشاء PostsController
- إنشاء جميع Controllers المطلوبة للصفحة الرئيسية

---

### 📁 lib/translations/

#### `app_translations.dart`
**الوظيفة**: إدارة ترجمات التطبيق
**المحتوى**:
- ترجمات العربية والإنجليزية
- مفاتيح الترجمة
- النصوص المستخدمة في التطبيق

**الأدوات المستخدمة**:
- GetX Translations
- Locale management

---

## الترابطات الرئيسية في المشروع

### 1. تدفق المصادقة
```
main.dart → AuthService → AuthController → AuthPage
                ↓
        Supabase Auth → Database
                ↓
        HomePage (عند النجاح)
```

### 2. تدفق المنشورات
```
FeedPage → PostsController → PostsService → Supabase
    ↓
PostCard → MediaCarousel → CachedNetworkImage
```

### 3. تدفق التعليقات
```
PostCard → CommentsPage → CommentsController → CommentsService
    ↓
CommentThread → Supabase Comments Table
```

### 4. إدارة الحالة
```
GetX Controllers ↔ GetX Services ↔ Supabase
        ↓
    UI Widgets (Obx/GetBuilder)
```

## نصائح للتطوير والصيانة

1. **إضافة ميزة جديدة**:
   - إنشاء Model في lib/models/
   - إنشاء Service في lib/services/
   - إنشاء Controller في lib/controllers/
   - إنشاء View في lib/views/
   - إضافة Route في app_routes.dart

2. **إدارة الأخطاء**:
   - استخدام ErrorHandler لمعالجة الأخطاء
   - عرض رسائل مفهومة للمستخدم
   - تسجيل الأخطاء للتطوير

3. **الأمان**:
   - استخدام AuthMiddleware لحماية الصفحات
   - التحقق من صحة البيانات في الـ Controllers
   - استخدام Row Level Security في Supabase

4. **الأداء**:
   - استخدام CachedNetworkImage للصور
   - تحميل البيانات بشكل تدريجي
   - استخدام GetX للتحكم الفعال في الذاكرة

هذا الدليل يوفر فهماً شاملاً لهيكل المشروع وكيفية عمل كل جزء منه والترابطات بينها.

---

## تفاصيل إضافية للملفات الرئيسية

### 📁 lib/models/ - تفاصيل النماذج

#### `post_model.dart`
**الوظيفة**: نموذج بيانات المنشور الشامل
**الخصائص الرئيسية**:
- **معرف المنشور**: id (String) - معرف فريد للمنشور
- **بيانات المستخدم**: userId, username, userProfileImage, isUserVerified
- **المحتوى**: caption (النص), mediaUrls (قائمة الصور), type (نوع المنشور)
- **التفاعل**: likesCount, commentsCount, sharesCount, viewsCount
- **الإعدادات**: commentsEnabled, likesVisible
- **التوقيت**: createdAt, updatedAt
- **الميزات المتقدمة**: trendingScore, isPremiumContent, hasPriorityInTrending
- **حالة المستخدم**: isLikedByCurrentUser

**الدوال المهمة**:
- `fromJson()`: تحويل البيانات من JSON إلى كائن
- `toJson()`: تحويل الكائن إلى JSON للإرسال
- `copyWith()`: إنشاء نسخة معدلة من المنشور

**الترابطات**:
- يستخدم في PostsController لإدارة المنشورات
- يستخدم في PostCard لعرض المنشور
- يتفاعل مع Supabase Posts table

#### `user_profile_model.dart`
**الوظيفة**: نموذج بيانات المستخدم
**الخصائص الرئيسية**:
- **المعرف**: id (String) - معرف فريد للمستخدم
- **البيانات الأساسية**: username, fullName, profileImageUrl
- **المعلومات الإضافية**: bio (السيرة الذاتية), isVerified (التحقق)
- **التوقيت**: createdAt, updatedAt

**الدوال المهمة**:
- `fromJson()`: تحويل من JSON
- `toJson()`: تحويل إلى JSON
- `copyWith()`: إنشاء نسخة معدلة
- `toString()`: عرض نصي للكائن
- `operator ==` و `hashCode`: للمقارنة بين المستخدمين

**الترابطات**:
- يستخدم في AuthController للمصادقة
- يستخدم في UserProfileController لعرض الملفات
- يتفاعل مع Supabase Users table

#### `comment_model.dart`
**الوظيفة**: نموذج بيانات التعليق
**الخصائص المتوقعة**:
- **المعرف**: id, postId, userId
- **المحتوى**: content, parentCommentId (للردود)
- **بيانات المستخدم**: username, userProfileImage
- **التوقيت**: createdAt, updatedAt
- **التفاعل**: likesCount, repliesCount

---

### 📁 lib/widgets/ - تفاصيل المكونات

#### `post_card.dart`
**الوظيفة**: مكون عرض المنشور مع تصميم متقدم
**الميزات الرئيسية**:

1. **تصميم متكيف**:
   - تصميم خاص للمنشورات النصية فقط
   - تصميم متناغم للمنشورات مع الصور
   - دعم الوضع الليلي والنهاري

2. **عرض المحتوى**:
   - عرض صورة المستخدم واسمه
   - دعم علامة التحقق للمستخدمين المميزين
   - عرض الموقع إذا كان متاحاً
   - عرض النص مع تنسيق جميل
   - عرض التاجز (hashtags) بتصميم مميز

3. **عرض الوسائط**:
   - دعم الصور المفردة والمتعددة
   - استخدام CachedNetworkImage لتحسين الأداء
   - معالجة أخطاء تحميل الصور
   - إمكانية عرض الصور بملء الشاشة

4. **التفاعل**:
   - أزرار الإعجاب والتعليق والمشاركة والحفظ
   - عرض عدد الإعجابات والتعليقات
   - تأثيرات بصرية عند التفاعل
   - قائمة خيارات إضافية (نسخ الرابط، الإبلاغ، الحذف)

5. **المعلومات الإضافية**:
   - عرض الوقت النسبي باستخدام timeago
   - عرض عدد المشاهدات
   - معاينة التعليقات

**الأدوات المستخدمة**:
- `CachedNetworkImage`: تحسين عرض الصور
- `timeago`: عرض الأوقات النسبية
- `PageView`: للتنقل بين الصور المتعددة
- `AnimatedContainer`: للتأثيرات البصرية
- `GestureDetector`: للتفاعل مع اللمس

**الترابطات**:
- يستقبل PostModel لعرض البيانات
- يتفاعل مع PostsController عبر callback functions
- يستخدم في FeedPage لعرض قائمة المنشورات

#### `comment_thread.dart`
**الوظيفة**: مكون عرض التعليق مع الردود المتداخلة
**الميزات المتوقعة**:
- عرض التعليق الأساسي
- عرض الردود بشكل متداخل
- إمكانية الرد على التعليقات
- خيارات الحذف والتعديل
- عرض معلومات المعلق

#### `media_carousel.dart`
**الوظيفة**: مكون عرض الصور المتعددة
**الميزات المتوقعة**:
- عرض الصور في شكل carousel
- مؤشرات الصور الحالية
- إمكانية التكبير والتصغير
- التنقل بالسحب أو النقر

#### `filter_tabs.dart`
**الوظيفة**: مكون تبويبات الفلترة
**الميزات المتوقعة**:
- تبويبات مختلفة (الكل، الأحدث، الأكثر شعبية)
- تغيير الفلتر النشط
- تحديث المحتوى حسب الفلتر المختار

#### `connectivity_widget.dart`
**الوظيفة**: مكون عرض حالة الاتصال
**الميزات المتوقعة**:
- مراقبة حالة الاتصال بالإنترنت
- عرض رسالة عند انقطاع الاتصال
- زر إعادة المحاولة
- تحديث تلقائي عند عودة الاتصال

---

### 📁 database/ - ملفات قاعدة البيانات

#### `database_schema.sql`
**الوظيفة**: هيكل قاعدة البيانات الأساسي
**المحتوى المتوقع**:
- جدول المستخدمين (users)
- جدول المنشورات (posts)
- جدول التعليقات (comments)
- جدول الإعجابات (likes)
- جدول المتابعة (followers)
- الفهارس والقيود

#### `app_specific_queries.sql`
**الوظيفة**: استعلامات خاصة بالتطبيق
**المحتوى المتوقع**:
- استعلامات جلب المنشورات مع التفاعل
- استعلامات البحث والفلترة
- استعلامات الإحصائيات
- استعلامات التحليلات

#### `database_maintenance.sql`
**الوظيفة**: صيانة قاعدة البيانات
**المحتوى المتوقع**:
- تنظيف البيانات القديمة
- تحديث الإحصائيات
- إعادة بناء الفهارس
- نسخ احتياطية

#### `DATABASE_SETUP_GUIDE.md`
**الوظيفة**: دليل إعداد قاعدة البيانات
**المحتوى المتوقع**:
- خطوات إنشاء قاعدة البيانات
- تشغيل ملفات SQL
- إعداد الأذونات
- اختبار الاتصال

---

### 📁 images/ - الأصول المرئية

#### `logo.png`
**الوظيفة**: شعار التطبيق
**الاستخدام**: في أيقونة التطبيق وشاشة البداية

#### `screenshots/`
**الوظيفة**: لقطات شاشة للتطبيق
**الملفات**:
- `login_screen.jpg`: شاشة تسجيل الدخول
- `signup_screen.jpg`: شاشة التسجيل
- `feed_page.jpg`: الصفحة الرئيسية
- `comments_screen.jpg`: شاشة التعليقات
- `profile_edit_screen.jpg`: تعديل الملف الشخصي
- `settings_screen.jpg`: شاشة الإعدادات
- `dark_mode.jpg`: الوضع الليلي

**الاستخدام**: في ملف README.md لعرض ميزات التطبيق

---

## أنماط التصميم المستخدمة

### 1. نمط MVC (Model-View-Controller)
```
Models (البيانات) ↔ Controllers (المنطق) ↔ Views (الواجهة)
```

### 2. نمط Repository
```
Controllers → Services → Supabase
```

### 3. نمط Dependency Injection
```
Bindings → GetX → Controllers/Services
```

### 4. نمط Observer
```
GetX Controllers → Reactive UI (Obx/GetBuilder)
```

---

## تدفق البيانات في التطبيق

### 1. تدفق المصادقة
```
AuthPage → AuthController → AuthService → Supabase Auth
    ↓
تحديث حالة المصادقة → التنقل للصفحة المناسبة
```

### 2. تدفق المنشورات
```
FeedPage → PostsController → PostsService → Supabase Database
    ↓
تحديث قائمة المنشورات → إعادة بناء UI
```

### 3. تدفق التعليقات
```
PostCard → CommentsPage → CommentsController → CommentsService
    ↓
تحديث التعليقات → إعادة بناء CommentThread
```

### 4. تدفق الملف الشخصي
```
ProfileEditPage → ProfileEditController → UserProfileService
    ↓
تحديث بيانات المستخدم → تحديث UI
```

---

## إرشادات الصيانة والتطوير

### إضافة ميزة جديدة:

1. **إنشاء Model**:
   ```dart
   // في lib/models/
   class NewFeatureModel {
     // خصائص البيانات
     // دوال التحويل (fromJson, toJson)
     // دوال المساعدة (copyWith)
   }
   ```

2. **إنشاء Service**:
   ```dart
   // في lib/services/
   class NewFeatureService extends GetxService {
     // دوال التفاعل مع قاعدة البيانات
     // معالجة الأخطاء
   }
   ```

3. **إنشاء Controller**:
   ```dart
   // في lib/controllers/
   class NewFeatureController extends GetxController {
     // متغيرات الحالة
     // دوال المنطق
     // ربط مع Service
   }
   ```

4. **إنشاء View**:
   ```dart
   // في lib/views/
   class NewFeaturePage extends StatelessWidget {
     // واجهة المستخدم
     // ربط مع Controller
   }
   ```

5. **إضافة Route**:
   ```dart
   // في lib/routes/app_routes.dart
   GetPage(
     name: '/new-feature',
     page: () => NewFeaturePage(),
     binding: NewFeatureBinding(),
   )
   ```

### أفضل الممارسات:

1. **إدارة الذاكرة**:
   - استخدام `onClose()` في Controllers لتنظيف الموارد
   - إلغاء الاشتراكات في Streams
   - التخلص من TextEditingControllers

2. **معالجة الأخطاء**:
   - استخدام try-catch في جميع العمليات
   - عرض رسائل خطأ مفهومة للمستخدم
   - تسجيل الأخطاء للتطوير

3. **الأداء**:
   - استخدام CachedNetworkImage للصور
   - تحميل البيانات بشكل تدريجي
   - استخدام ListView.builder للقوائم الطويلة

4. **الأمان**:
   - التحقق من صحة البيانات في الـ Frontend والـ Backend
   - استخدام Row Level Security في Supabase
   - عدم تخزين معلومات حساسة محلياً

5. **تجربة المستخدم**:
   - عرض مؤشرات التحميل
   - معالجة حالات عدم وجود بيانات
   - دعم الوضع الليلي
   - دعم اللغات المتعددة

---

## الخلاصة

هذا المشروع يمثل تطبيق Instagram Clone متكامل يستخدم أحدث التقنيات في تطوير تطبيقات Flutter. البنية المعمارية المستخدمة تضمن:

- **قابلية الصيانة**: فصل الاهتمامات وتنظيم الكود
- **قابلية التوسع**: سهولة إضافة ميزات جديدة
- **الأداء**: استخدام أفضل الممارسات لتحسين الأداء
- **تجربة المستخدم**: واجهة مستخدم حديثة ومتجاوبة

الدليل يوفر فهماً شاملاً لكل جزء من المشروع ويساعد في التطوير والصيانة المستقبلية.